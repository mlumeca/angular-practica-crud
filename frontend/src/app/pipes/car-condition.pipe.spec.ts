import { CarConditionPipe } from './car-condition.pipe';

describe('CarConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new CarConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
