import { ReusableButtonDirective } from './reusable-button.directive';

describe('ReusableButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ReusableButtonDirective();
    expect(directive).toBeTruthy();
  });
});
